{
  "name": "waybar-custom",
  "version": "0.1.0",
  "scripts": {
    "build:after": "rm -rf dist/*.js style.css && cp dist/style.css config/style.css",
    "build:dev": "webpack --mode=development && npm run build:after",
    "build": "webpack --mode=production && npm run build:after",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "css-loader": "^7.1.2",
    "mini-css-extract-plugin": "^2.9.2",
    "sass": "^1.87.0",
    "sass-loader": "^16.0.5",
    "style-loader": "^4.0.0",
    "webpack": "^5.99.6",
    "webpack-cli": "^6.0.1"
  }
}
